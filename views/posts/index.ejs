<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mudhakkarat - Your Journal</title>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;400&family=Playfair+Display&display=swap" rel="stylesheet">
    <style>
        :root {
            --old-ivory: #F0E6D2; /* Antique Ivory */
            --burnt-amber: #9C5C3D; /* Deep rich amber */
            --sage: #A6B79A; /* Soft olive green */
            --deep-brown: #5C3D29; /* Dark walnut brown */
            --golden-sand: #D8B35C; /* Soft vintage gold */
            --saffron: #F4A300; /* Soft saffron yellow */
            --dark-clay: #8A6E4D; /* Muted clay */
            --faded-pink: #F1D1C1; /* Faded, warm pink */
        }

        body {
            font-family: 'Raleway', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--old-ivory);
            color: var(--deep-brown);
            line-height: 1.6;
            background-image: url('https://www.transparenttextures.com/patterns/aged-paper.png');
            background-size: 100%;
            border: 10px solid var(--burnt-amber); /* Adding border to the body */
            box-sizing: border-box;
        }

        header {
            background-color: var(--sage);
            color: var(--old-ivory);
            text-align: center;
            padding: 30px;
            font-size: 2rem;
            font-family: 'Playfair Display', serif;
            letter-spacing: 2px;
            border-bottom: 3px solid var(--golden-sand);
        }

        .container {
            max-width: 900px;
            margin: 20px auto;
            padding: 20px;
        }

        /* Center the "Add New Post" button */
        .add-new-post-button {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .add-new-post-button a {
            padding: 20px 40px;  /* Larger padding for a bigger button */
            background-color: var(--golden-sand);
            color: var(--old-ivory);
            text-decoration: none;
            font-size: 1.3rem;    /* Larger font size */
            font-weight: bold;
            border-radius: 8px;   /* Rounded corners */
            transition: background-color 0.3s ease;
        }

        .add-new-post-button a:hover {
            background-color: var(--saffron);
        }

        .search-bar {
            display: flex;
            justify-content: center;
            margin-bottom: 30px;
        }

        .search-bar input {
            width: 80%;
            padding: 12px;
            font-size: 1.1rem;
            border: 1px solid var(--dark-clay);
            border-radius: 6px;
            background-color: var(--faded-pink);
            color: var(--deep-brown);
        }

        .posts {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 25px;
        }

        .post-card {
            background-color: var(--old-ivory);
            border: 1px solid var(--dark-clay);
            border-radius: 8px;
            padding: 20px;
            box-shadow: 4px 4px 12px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
            text-decoration: none;
            color: var(--deep-brown);
            background-image: linear-gradient(to bottom, rgba(255, 255, 255, 0.9), rgba(255, 255, 255, 0.7)), 
                              url('https://www.transparenttextures.com/patterns/old-wall.png');
            background-size: cover;
        }

        .post-card:hover {
            transform: translateY(-5px);
            box-shadow: 6px 6px 15px rgba(0, 0, 0, 0.2);
        }

        .post-card h2 {
            font-family: 'Playfair Display', serif;
            font-size: 1.6rem;
            margin-bottom: 12px;
            color: var(--burnt-amber);
        }

        .post-card p {
            font-size: 1rem;
            color: var(--dark-clay);
            margin-bottom: 15px;
        }

        .post-card .tags {
            font-size: 0.9rem;
            color: var(--golden-sand);
        }

        .post-card .timestamp {
            font-size: 0.9rem;
            color: var(--sage);
            text-align: right;
        }

        .pagination {
            display: flex;
            justify-content: center;
            margin: 30px 0;
        }

        .pagination a {
            margin: 0 8px;
            padding: 12px 20px;
            background-color: var(--golden-sand);
            color: var(--old-ivory);
            text-decoration: none;
            border-radius: 6px;
            font-weight: bold;
        }

        .pagination a:hover {
            background-color: var(--saffron);
        }

        footer {
            text-align: center;
            padding: 20px;
            background-color: var(--sage);
            color: var(--old-ivory);
        }

    </style>
</head>
<body>

    <header>
        <h1>Mudhakkarat <%= user.username %> 🤎 </h1>
    </header>
    <%- include('../partials/_navbar.ejs') %>

    <div class="container">
        <!-- Add New Post Button centered -->
        <div class="add-new-post-button">
            <a href="./posts/new">Add a new post</a>
        </div>
        <br><br>

        <!-- Search Bar -->
        <div class="search-bar">
            <input type="text" placeholder="Search for tags or content..." id="search-input">
        </div>

        <!-- Posts -->
        <div class="posts">
            <% posts.forEach(post => { %>
                <a href="/users/<%= user._id %>/posts/<%= post._id %>" class="post-card">
                    <h2><%= post.title %></h2>
                    <p><%= post.content.slice(0, 100) %>...</p>
                    <div class="tags"><%= post.tags.join(', ') %></div>
                    <div class="timestamp"><%= post.timestamp.toLocaleString() %></div>
                </a>
            <% }) %>
        </div>

        <!-- Pagination -->
        <div class="pagination">
            <a href="#">Previous</a>
            <a href="#">Next</a>
        </div>

    </div>

    <footer>
        <p>© 2025 Mudhakkarat</p>
    </footer>

</body>
</html>
