<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= post.title %> </title>
  
  <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;400&family=Playfair+Display&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --old-ivory: #F0E6D2;
      --burnt-amber: #9C5C3D;
      --sage: #A6B79A;
      --deep-brown: #5C3D29;
      --golden-sand: #D8B35C;
      --saffron: #F4A300;
      --dark-clay: #8A6E4D;
    }

    body {
      font-family: 'Raleway', sans-serif;
      margin: 0;
      padding: 0;
      background-color: var(--dark-clay);
      color: var(--deep-brown);
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      box-sizing: border-box;
      text-align: center;
    }

    h1 {
      font-family: 'Playfair Display', serif;
      font-size: 3.5rem;
      color: var(--golden-sand);
      padding: 20px 30px;
      margin-bottom: 30px;
      border-bottom: 3px solid var(--sage);
      letter-spacing: 2px;
      text-transform: uppercase;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
    }

    .container {
      width: 80%;
      max-width: 1000px;
      background-color: var(--old-ivory);
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      text-align: left;
      border-left: 12px solid var(--burnt-amber);
      border-top: 12px solid var(--burnt-amber);
    }

    .image-container {
      flex: 1;
      min-width: 250px;
      max-width: 400px;
      background-color: var(--sage);
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .image-container img {
      max-width: 100%;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .content-container {
      flex: 2;
      padding: 0 30px;
      background-color: var(--old-ivory);
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .content-container p {
      line-height: 1.8;
      font-size: 1.2rem;
      margin-bottom: 20px;
      font-weight: 400;
      color: var(--deep-brown);
    }

    .content-container strong {
      font-weight: 600;
      color: var(--sage);
    }

    .btn {
      display: inline-block;
      padding: 12px 25px;
      background-color: var(--burnt-amber);
      color: var(--old-ivory);
      text-decoration: none;
      border-radius: 8px;
      font-size: 1.1rem;
      margin-top: 30px;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .btn:hover {
      background-color: var(--saffron);
      color: var(--old-ivory);
      transform: translateY(-2px);
    }

    .delete-form {
      margin-top: 20px;
    }

    .delete-btn {
      background-color: var(--sage);
      color: var(--old-ivory);
      padding: 12px 25px;
      border-radius: 8px;
      font-size: 1.1rem;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .delete-btn:hover {
      background-color: var(--burnt-amber);
      transform: translateY(-2px);
    }
/* Adjusted CSS for buttons to be on the same line */
.page-lines {
  display: flex;
  justify-content: space-between;
  margin-top: 30px;
  padding-top: 20px;
}

.page-lines .btn {
  margin: 0 10px 0 0; /* Space between the buttons */
}

.page-lines .delete-btn {
  background-color: var(--sage);
  color: var(--old-ivory);
  padding: 12px 25px;
  border-radius: 8px;
  font-size: 1.1rem;
  transition: background-color 0.3s ease, transform 0.2s ease;
}

.page-lines .delete-btn:hover {
  background-color: var(--burnt-amber);
  transform: translateY(-2px);
}


    .ornate-border {
      border-top: 2px solid var(--golden-sand);
      margin-top: 15px;
      padding-top: 10px;
      position: relative;
    }

    .ornate-border::before {
      content: '❖';
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
      top: -10px;
      font-size: 1.5rem;
      color: var(--golden-sand);
    }

    @media (max-width: 768px) {
      .container {
        flex-direction: column;
        text-align: center;
      }

      .image-container {
        margin-bottom: 20px;
        max-width: 100%;
      }

      .content-container {
        text-align: left;
      }

      h1 {
        font-size: 2.2rem;
      }

      .btn, .delete-btn {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>

  <%- include('../partials/_navbar.ejs') %>

  <div class="container">
    <div class="image-container">
      <% if (post.imageUrl) { %>
        <img src="<%= post.imageUrl %>" alt="Image for <%= post.title %>" />
      <% } else { %>
        <p>No image available.</p>
      <% } %>
    </div>

    <div class="content-container">
      <h1><%= post.title %></h1>

      <div class="content-section">
        <% if (post.content) { %>
          <p><strong>Content:</strong> <%= post.content %></p>
        <% } else { %>
          <p><strong>Content:</strong> No content provided.</p>
        <% } %>
      </div>

      <div class="content-section">
        <p><strong>Current Mood:</strong> <%= post.mood %></p>
      </div>

      <div class="content-section">
        <% if (post.tags && post.tags.length > 0) { %>
          <p><strong>Tags:</strong> <%= post.tags.join(', ') %></p>
        <% } else { %>
          <p><strong>Tags:</strong> No tags available.</p>
        <% } %>
      </div>

      <div class="page-lines">
        <!-- Edit and Delete buttons on the same line -->
        <a class="btn" href="/users/<%= user._id %>/posts/<%= post._id %>/edit">Edit this post</a>
      
        <form class="delete-form" action="/users/<%= user._id %>/posts/<%= post._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
          <button class="btn delete-btn" type="submit">Delete this post</button>
        </form>
      </div>
      

      <div class="ornate-border"></div>
    </div>
  </div>

</body>
</html>
